# InkedArtiq Studio Manager

Project tracker + invoice generator for InkedArtiq Studio.
Static site — no build step, no dependencies to install.

## Local preview
Just open `index.html` in a browser, or serve the folder:
    npx serve .

## Deploy
Import this repo into Vercel with the "Other" framework preset (no build command).
